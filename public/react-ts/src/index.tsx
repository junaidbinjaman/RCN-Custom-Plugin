import React from 'react';
import ReactDOM from 'react-dom/client';
import './style/index.scss';
import MultiStepForm from './canvas/multiStepForm/MultiStepForm';

const multiStepForm = ReactDOM.createRoot(
  document.getElementById('kshdfsdh') as HTMLElement
);

multiStepForm.render(
  <React.StrictMode>
    <MultiStepForm />
  </React.StrictMode>
);